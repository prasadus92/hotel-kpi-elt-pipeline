# Python
.venv/
__pycache__/
*.py[cod]
.DS_Store

# DuckDB database (regenerated by the EL step on every run)
*.duckdb
*.duckdb.wal

# dbt build artifacts
dbt/hotel_kpi/target/
dbt/hotel_kpi/dbt_packages/
dbt/hotel_kpi/logs/
logs/

# Note: data/ and output/ ARE committed on purpose —
# the input data makes the pipeline reproducible, and the pre-generated
# output CSV is part of the required deliverable.

# dbt anonymous usage stamp
dbt/hotel_kpi/.user.yml
